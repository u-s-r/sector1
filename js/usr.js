if ( 'undefined' === typeof window.usr ) {
	window.usr = {};
}

( function( usr ) {
	'use strict';

	usr.map = {
		icon: '../assets/app/img/icon-marker.png',
		position: {
			lat: 44.4373056,
			lng: 26.0660234
		}
	};
})( usr );
